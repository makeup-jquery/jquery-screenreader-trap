{
  "name": "jquery-screenreader-trap",
  "description": "Prevents screen reader's virtual cursor from leaving the confines of an element",
  "version": "0.3.0",
  "scripts": {
    "start": "npm run lasso && gulp",
    "tdd": "karma start --autoWatch true --singleRun false",
    "test": "karma start --autoWatch false --singleRun true",
    "prepublish": "npm run clean && npm run lint && npm run test && npm run build",
    "build": "npm run jsdoc && npm run minify",
    "clean": "rimraf jsdoc test_reports .cache build lint.txt",
    "fix": "eslint jquery.screenreadertrap.js --fix",
    "jsdoc": "jsdoc jquery.screenreadertrap.js -d jsdoc",
    "lasso": "lasso browser.json --output-dir docs --minify",
    "lint": "eslint jquery.screenreadertrap.js > lint.txt",
    "minify": "jsmin -o jquery.screenreadertrap.min.js jquery.screenreadertrap.js"
  },
  "pre-commit": [
    "clean",
    "lint",
    "test",
    "build"
  ],
  "author": "Ian McBurnie <ianmcburnie@hotmail.com>",
  "homepage": "https://github.com/ianmcburnie/jquery-screenreader-trap",
  "bugs": {
    "url": "https://github.com/ianmcburnie/jquery-screenreader-trap/issues"
  },
  "keywords": [
    "jquery-plugin",
    "ecosystem:jquery",
    "ebay",
    "accessibility",
    "a11y"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ianmcburnie/jquery-screenreader-trap.git"
  },
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2",
    "child_process": "^1",
    "coveralls": "^2",
    "eslint": "^3",
    "eslint-config-google": "~0.6.0",
    "gulp": "^3.9.1",
    "jasmine-core": "^2",
    "jquery": ">=1.11",
    "jquery-migrate": "^3",
    "jsdoc": "^3",
    "jsmin": "^1",
    "karma": "^1",
    "karma-coverage": "^1",
    "karma-html-reporter": "~0.2",
    "karma-jasmine": "^1",
    "karma-junit-reporter": "^1",
    "karma-phantomjs-launcher": "^1",
    "phantomjs-prebuilt": "^2",
    "pre-commit": "^1",
    "rimraf": "^2"
  },
  "peerDependencies": {
    "jquery": ">=1.11"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
